import {interfaces} from "@/core";
import {UI} from "@/categories"

export const definition : interfaces.ComponentDefinition  = {

    id: "component_structure",
    name: "componentStructure",
    version: "v1.0.0" ,

    category: UI.basic

}